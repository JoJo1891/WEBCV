<form method="post" action="{{ path('center_interest_delete', {'id': center_interest.id,'idcv': cv}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ center_interest.id) }}">
    <button class="btn">Delete</button>
</form>
