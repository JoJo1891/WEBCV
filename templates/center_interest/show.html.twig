{% extends 'base.html.twig' %}

{% block title %}CenterInterest{% endblock %}

{% block body %}
    <h1>CenterInterest</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ center_interest.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ center_interest.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('center_interest_index') }}">back to list</a>

    <a href="{{ path('center_interest_edit', {'id': center_interest.id}) }}">edit</a>

    {{ include('center_interest/_delete_form.html.twig') }}
{% endblock %}
