{% extends 'base.html.twig' %}

{% block title %}ContactInformation{% endblock %}

{% block body %}
    <h1>ContactInformation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contact_information.id }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ contact_information.adresse }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ contact_information.numero }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ contact_information.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('contact_information_index') }}">back to list</a>

    <a href="{{ path('contact_information_edit', {'id': contact_information.id}) }}">edit</a>

    {{ include('contact_information/_delete_form.html.twig') }}
{% endblock %}
