{% extends 'base.html.twig' %}

{% block title %}Training{% endblock %}

{% block body %}
    <h1>Training</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ training.id }}</td>
            </tr>
            <tr>
                <th>Diplomatitle</th>
                <td>{{ training.diplomatitle }}</td>
            </tr>
            <tr>
                <th>School</th>
                <td>{{ training.school }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ training.city }}</td>
            </tr>
            <tr>
                <th>Yearofgraduation</th>
                <td>{{ training.yearofgraduation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('training_index') }}">back to list</a>

    <a href="{{ path('training_edit', {'id': training.id}) }}">edit</a>

    {{ include('training/_delete_form.html.twig') }}
{% endblock %}
