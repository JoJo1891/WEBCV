{% extends 'base.html.twig' %}

{% block title %}InfosPerso{% endblock %}

{% block body %}
    <h1>InfosPerso</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ infos_perso.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ infos_perso.firstname }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ infos_perso.name }}</td>
            </tr>
            <tr>
                <th>Jobtitle</th>
                <td>{{ infos_perso.jobtitle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('infos_perso_index') }}">back to list</a>

    <a href="{{ path('infos_perso_edit', {'id': infos_perso.id}) }}">edit</a>

    {{ include('infos_perso/_delete_form.html.twig') }}
{% endblock %}
