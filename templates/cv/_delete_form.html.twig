<form method="post" action="{{ path('cv_delete', {'id': cv}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce CV?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cv) }}">
    <div class="row right">
    	<button class="red waves-effect waves-light btn">Delete</button>
    </div>
</form>
