

{{ form_start(form) }}
    {#{ form_widget(form) }#}
    {{ form_row(form.name) }}
    <button  class="btn btn-large waves-effect waves-light">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
