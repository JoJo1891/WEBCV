{% extends 'base.html.twig' %}

{% block title %}ProfessionalCourse{% endblock %}

{% block body %}
    <h1>ProfessionalCourse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ professional_course.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ professional_course.title }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ professional_course.city }}</td>
            </tr>
            <tr>
                <th>Period</th>
                <td>{{ professional_course.period }}</td>
            </tr>
            <tr>
                <th>Skillsacquired</th>
                <td>{{ professional_course.skillsacquired }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('professional_course_index') }}">back to list</a>

    <a href="{{ path('professional_course_edit', {'id': professional_course.id}) }}">edit</a>

    {{ include('professional_course/_delete_form.html.twig') }}
{% endblock %}
